import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "react.typescript.template",
    appName: "react-typescript-template",
    webDir: "build",
    bundledWebRuntime: false
};

export default config;
